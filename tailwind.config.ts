/**
 * SkyIndia uses Tailwind CSS v4 with CSS-first configuration.
 * Theme tokens, fonts, and colors are defined in src/index.css via @theme.
 * Vite integration: @tailwindcss/vite plugin in vite.config.ts
 */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
}
